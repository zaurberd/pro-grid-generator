# Set your production URL for SEO (canonical URLs, sitemap, Open Graph).
# On Vercel, this is auto-detected from VERCEL_URL if unset.
NEXT_PUBLIC_BASE_URL=https://your-domain.com

# Amplitude browser analytics. Leave empty to disable tracking.
NEXT_PUBLIC_AMPLITUDE_API_KEY=
